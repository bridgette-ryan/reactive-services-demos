server:
  port: 8081

spring:
  r2dbc:
    name: reactive-users
    url: r2dbc:postgresql://localhost:5455
    username: reactive-web
    password: reactive

springdoc:
  api-docs:
    path: /api-docs

config:
  hosts:
    order-service: http://localhost:8082